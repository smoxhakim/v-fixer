# Frontend (`frontend/`)

The Next.js application source lives in this directory (`app/`, `components/`, `lib/`, etc.).

Repository-wide documentation (architecture, backend, and how to run both services) is in **`DOCUMENTATION.md`** at the **repository root**.

**Run from repo root:** `npm run dev` (delegates to this folder) or `cd frontend && npm run dev`.
